[package]
name = "trustforge-nf2-zero-capability-broker"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]

[features]
adversarial-test-hooks = []

[profile.release]
codegen-units = 1
lto = true
panic = "abort"
strip = "symbols"
