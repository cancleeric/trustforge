# Design

> See requirements.md for context.

Compare each methodology listed in `devlog references.html` against actual implementation in `src/`. Produce a structured audit document and an automated checker script.
