# Design

> See requirements.md for context.

Simple path unification: all references to `out/training-data/` are changed to `data/training/` across the codebase (backfill.py, web.py, cli.py, tests).
